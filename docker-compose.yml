version: '3.8'

services:
  3d-print-analyzer:
    image: ghcr.io/atomique13/3d-print-cost-analyzer:latest
    ports:
      - "8080:80"
    volumes:
      # Mount data.json for persistent storage across devices
      - ./data.json:/app/data.json